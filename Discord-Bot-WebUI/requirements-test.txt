# Testing dependencies
# Install with: pip install -r requirements.txt -r requirements-test.txt

# Core testing framework
#
# pytest 9 (CVE-2025-71176, insecure temp directory) could not be bumped alone:
# exactly two plugins carried a `pytest<9` ceiling -- pytest-asyncio and
# pytest-playwright -- so Dependabot's pytest-only PR resolved to
# ResolutionImpossible. The other four here have no upper bound and stay put;
# bumping them too would be churn, not a fix.
#
# pytest-asyncio 0.24 -> 1.4 crosses a major: 1.0 deleted the `event_loop`
# fixture. Nothing here overrides it, and pytest.ini already declares both
# `asyncio_mode = auto` and `asyncio_default_fixture_loop_scope = function`,
# which is what 1.x expects to be explicit.
pytest==9.1.1
pytest-cov==5.0.0
pytest-flask==1.3.0
pytest-mock==3.15.1
pytest-asyncio==1.4.0
pytest-timeout==2.4.0
pytest-xdist==3.8.0  # parallel test execution

# Test data generation
factory-boy==3.3.3
faker==30.8.1

# Coverage reporting
coverage==7.15.2

# HTTP mocking
responses==0.26.2

# Time mocking
freezegun==1.5.5

# Database testing
# pytest-postgresql==6.0.0  # Disabled due to psycopg dependency issues

# Performance testing
pytest-benchmark==5.2.3

# Browser automation & visual testing
# pytest-playwright 0.4.4 pinned `pytest<9.0.0`; 0.8.0 lifts it and drags
# playwright forward with it. No workflow runs these, so the bump only affects
# local browser runs.
playwright==1.61.0
pytest-playwright==0.8.0
pillow>=12.3.0              # Screenshot comparison
pixelmatch==0.4.0           # Visual diff
cssutils==2.15.0            # CSS parsing and analysis

# SQL DDL validation
# tests/test_public_site_runtime.py::TestDDL parses the public-site .sql files as
# Postgres. It was written against a locally-installed sqlglot and used a bare
# `import sqlglot`, so it failed outright the first time CI ran that file.
# Listed here so the check actually runs; the test also importorskips so a
# missing copy degrades to a skip rather than a failure.
sqlglot>=25,<28
